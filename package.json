{
  "name": "workday-sla-calculator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check dist/calculations.js && node --check dist/app.js && node --test tests/*.test.mjs"
  }
}
